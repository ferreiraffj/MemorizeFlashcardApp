<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.MainActivity">

    <!-- Conteúdo principal (mantém o CoordinatorLayout original) -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appBarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/toolbarWhiteBg">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar_main"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/toolbarWhiteBg"
                app:navigationIcon="@drawable/ic_menu">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="MEMORIZE APP"
                    android:textColor="@color/toolbarTitleBlue"
                    android:textSize="22sp"
                    android:textStyle="bold" />
            </androidx.appcompat.widget.Toolbar>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="100dp"
                android:background="@color/blueStripe">
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:gravity="center"
                    android:orientation="horizontal"
                    android:padding="16dp">
                    <!-- Container da Ofensiva -->
                    <LinearLayout
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:orientation="horizontal"
                        android:gravity="start|center_vertical">

                        <ImageView
                            android:id="@+id/iv_ofensiva_icon"
                            android:layout_width="48dp"
                            android:layout_height="48dp"
                            android:src="@drawable/ic_fogo_apagado"
                            tools:src="@drawable/ic_fogo_aceso"/>

                        <TextView
                            android:id="@+id/tv_ofensiva_header"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="1dp"
                            android:text="0"
                            android:textColor="@color/white"
                            android:textSize="32dp"/>
                    </LinearLayout>
                    <androidx.constraintlayout.widget.ConstraintLayout
                        android:id="@+id/ranking_container"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1.5"> <!-- Dando um pouco mais de espaço para o ranking -->

                        <!-- Imagem do Ranking Atual -->
                        <ImageView
                            android:id="@+id/iv_current_rank_icon"
                            android:layout_width="48dp"
                            android:layout_height="48dp"
                            app:layout_constraintStart_toStartOf="parent"
                            app:layout_constraintTop_toTopOf="parent"
                            app:layout_constraintBottom_toBottomOf="parent"
                            tools:src="@drawable/ic_rank_bronze" /> <!-- Imagem de exemplo -->

                        <!-- Imagem do Próximo Ranking -->
                        <ImageView
                            android:id="@+id/iv_next_rank_icon"
                            android:layout_width="48dp"
                            android:layout_height="48dp"
                            app:layout_constraintEnd_toEndOf="parent"
                            app:layout_constraintTop_toTopOf="parent"
                            app:layout_constraintBottom_toBottomOf="parent"
                            tools:src="@drawable/ic_rank_prata" /> <!-- Imagem de exemplo -->

                        <!-- Barra de Progresso de XP -->
                        <ProgressBar
                            android:id="@+id/progress_bar_xp"
                            style="?android:attr/progressBarStyleHorizontal"
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="15dp"
                            android:layout_marginStart="8dp"
                            android:layout_marginEnd="8dp"
                            android:progressTint="@color/white"
                            android:progressBackgroundTint="@color/grayTxtAuth"
                            app:layout_constraintStart_toEndOf="@id/iv_current_rank_icon"
                            app:layout_constraintEnd_toStartOf="@id/iv_next_rank_icon"
                            app:layout_constraintTop_toTopOf="parent"
                            tools:progress="40" />

                        <!-- Texto do XP (Ex: 40/100 XP) -->
                        <TextView
                            android:id="@+id/tv_xp_progress"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:textColor="@android:color/white"
                            android:textSize="12sp"
                            app:layout_constraintStart_toStartOf="@id/progress_bar_xp"
                            app:layout_constraintEnd_toEndOf="@id/progress_bar_xp"
                            app:layout_constraintTop_toBottomOf="@id/progress_bar_xp"
                            tools:text="40 / 100 XP" />

                    </androidx.constraintlayout.widget.ConstraintLayout>
                </LinearLayout>
            </FrameLayout>
        </com.google.android.material.appbar.AppBarLayout>

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipe_refresh_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recycler_view_decks"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:padding="16dp"
                    tools:listitem="@layout/item_baralho" />

                <TextView
                    android:id="@+id/text_view_no_decks_message"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="Adicione baralhos para que apareçam aqui!"
                    android:textColor="@color/grayTxtAuth"
                    android:textSize="18sp"
                    android:visibility="gone" />
            </FrameLayout>

        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp">

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/fab_create_deck"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="invisible"
                app:backgroundTint="@color/colorFab"
                app:fabSize="normal"
                app:srcCompat="@drawable/ic_create_deck"
                app:tint="@android:color/white"
                app:layout_constraintBottom_toTopOf="@+id/fab_add_card"
                app:layout_constraintEnd_toEndOf="parent"
                android:layout_marginBottom="16dp" />

            <TextView
                android:id="@+id/text_view_create_deck_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:background="@drawable/btn_arredondado"
                android:padding="8dp"
                android:text="Criar baralho"
                android:visibility="invisible"
                app:layout_constraintBottom_toBottomOf="@+id/fab_create_deck"
                app:layout_constraintEnd_toStartOf="@+id/fab_create_deck"
                app:layout_constraintTop_toTopOf="@+id/fab_create_deck" />

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/fab_add_card"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="invisible"
                app:backgroundTint="@color/colorFab"
                app:fabSize="normal"
                app:srcCompat="@drawable/ic_add_card"
                app:tint="@android:color/white"
                app:layout_constraintBottom_toTopOf="@+id/fab_add_deck"
                app:layout_constraintEnd_toEndOf="parent"
                android:layout_marginBottom="16dp" />

            <TextView
                android:id="@+id/text_view_add_card_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:background="@drawable/btn_arredondado"
                android:padding="8dp"
                android:text="Adicionar"
                android:visibility="invisible"
                app:layout_constraintBottom_toBottomOf="@+id/fab_add_card"
                app:layout_constraintEnd_toStartOf="@+id/fab_add_card"
                app:layout_constraintTop_toTopOf="@+id/fab_add_card" />

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/fab_add_deck"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="Adicionar novo baralho"
                android:src="@drawable/ic_add"
                app:backgroundTint="@color/colorFab"
                app:tint="@android:color/white"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- Menu lateral (NavigationView) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:itemIconTint="@color/colorPrimary"
        app:itemTextAppearance="@style/NavigationViewTextStyle"
        app:menu="@menu/drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>